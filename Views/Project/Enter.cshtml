@model ScrumApp__Juro_.ViewModels.ProjectsViewModel

@{
    ViewData["Title"] = "Project Details";
}


<head>
    <link rel="stylesheet" href="~/css/EnterStyles.css" asp-append-version="true" />
    <link rel="stylesheet" href="~/css/ProjectDetailsStyle.css" />
    <link rel="stylesheet" href="~/css/LayoutStyles.css" />
</head>


<div class="container mt-5">
    <h1 class="mb-4">Project - @Model.Project.ProjectID</h1>

    <div class="card shadow-sm mb-4">
        <div class="card-body">
            <h2 class="card-title">Title</h2>
            <p class="card-text">@Model.Project.Title</p>

            <h2 class="card-title">Description</h2>
            <p class="card-text">@Model.Project.Description</p>
        </div>
    </div>

    <h2 class="mb-3">Status</h2>

    <h4 class="mb-2">Not Started / Backlog</h4> <!-- NOT STARTED SECTION -->
    <p>
        <a asp-action="CreateModule" asp-route-id="@Model.Project.ProjectID" class="btn btn-success">Create Module</a>
    </p>

    <div class="row mb-4">
        <div class="col-md-4">
            <div class="card shadow-sm">
                <div class="card-body text-center">
                    <h5 class="card-title">Modules</h5>  <!-- MODULE SECTION -->
                    @if (Model.Modules.FindAll(m => m.Status == "Not Started").Count != 0)
                    {
                        <ul class="list-group list-group-flush">
                            @foreach (var module in Model.Modules.FindAll(m => m.Status == "Not Started"))
                            {
                                <li class="list-group-item ItemCard2 ItemCard-NotStarted">

                                    <!-- Modal start -->
                                    <div class="modal fade" id="moduleModal-@module.ModuleID" tabindex="-1" aria-labelledby="moduleModalLabel-@module.ModuleID" aria-hidden="true">
                                        <div class="modal-dialog modal-dialog-centered">
                                            <div class="modal-content">
                                                <div class="modal-header">
                                                    <h5 class="modal-title" id="viewModalLabel-@module.ModuleID">Module Details</h5>
                                                    <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                                                </div>
                                                <div class="modal-body text-start">
                                                    <p><strong>Project ID:</strong> @module.ProjectID</p>
                                                    <p><strong>Module ID:</strong> @module.ModuleID</p>
                                                    <p><strong>Title:</strong> @module.Title</p>
                                                    <p><strong>Description:</strong> @module.Description</p>
                                                    <p><strong>Status:</strong> @module.Status</p>
                                                </div>
                                                <div class="modal-footer">
                                                    <a asp-controller="SubModule" asp-action="Create" asp-route-id="@module.ModuleID" class="btn btn-primary">
                                                        Add Submodule
                                                    </a>
                                                    <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Close</button>
                                                </div>
                                            </div>
                                        </div>
                                    </div>
                                    <!-- Modal end -->

                                    <p class="mb-1"><strong>Title:</strong> @module.Title</p>
                                    <div class="mt-2">
                                        <form asp-controller="Module" asp-action="MoveStatusUp" asp-route-id="@module.ModuleID" method="post" class="d-inline">
                                            @Html.AntiForgeryToken()
                                            <button type="submit" class="btn btn-sm btn-primary" title="Move Up">
                                                <i class="bi bi-arrow-up"></i>
                                            </button>
                                        </form>
                                        <form asp-controller="Module" asp-action="Delete" asp-route-id="@module.ModuleID" method="post" class="d-inline delete-form ms-1">
                                            @Html.AntiForgeryToken()
                                            <button type="submit" class="btn btn-sm btn-danger" title="Remove">
                                                <i class="bi bi-x"></i>
                                            </button>
                                        </form>

                                        <a href="#" class="btn btn-sm btn-info ms-1" data-bs-toggle="modal" data-bs-target="#moduleModal-@module.ModuleID" title="View">
                                            <i class="bi bi-eye"></i>
                                        </a>

                                    </div>
                                </li>
                            }
                        </ul>
                    }
                    else
                    {
                        <div class="alert alert-info shadow-sm mt-3">
                            No Modules created
                        </div>
                    }
                </div>
            </div>
        </div>

        <div class="col-md-4">
            <div class="card shadow-sm">
                <div class="card-body text-center">
					<h5 class="card-title">Submodules</h5> <!-- SUBMODULE SECTION -->
                    @if (Model.SubModules.FindAll(m => m.Status == "Not Started").Count != 0)
                    {
                        <ul class="list-group list-group-flush">
                            @foreach (var submodule in Model.SubModules.FindAll(m => m.Status == "Not Started"))
                            {
                                <li class="list-group-item ItemCard2 ItemCard-NotStarted">

                                    <!-- Modal start -->
                                    <div class="modal fade" id="submoduleModal-@submodule.SubModuleID" tabindex="-1" aria-labelledby="submoduleModalLabel-@submodule.SubModuleID" aria-hidden="true">
                                        <div class="modal-dialog modal-dialog-centered">
                                            <div class="modal-content">
                                                <div class="modal-header">
                                                    <h5 class="modal-title" id="viewModalLabel-@submodule.SubModuleID">Submodule Details</h5>
                                                    <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                                                </div>
                                                <div class="modal-body text-start">
                                                    <p><strong>Module ID:</strong> @submodule.ModuleID</p>
                                                    <p><strong>Submodule ID:</strong> @submodule.SubModuleID</p>
                                                    <p><strong>Title:</strong> @submodule.Title</p>
                                                    <p><strong>Description:</strong> @submodule.Description</p>
                                                    <p><strong>Status:</strong> @submodule.Status</p>
                                                </div>
                                                <div class="modal-footer">
                                                    <a asp-controller="Task" asp-action="Create" asp-route-id="@submodule.SubModuleID" class="btn btn-primary">
                                                        Add task
                                                    </a>
                                                    <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Close</button>
                                                </div>
                                            </div>
                                        </div>
                                    </div>
                                    <!-- Modal end -->

                                    <p class="mb-1"><strong>Title:</strong> @submodule.Title</p>
                                    <div class="mt-2">
                                        <form asp-controller="Submodule" asp-action="MoveStatusUp" asp-route-id="@submodule.SubModuleID" method="post" class="d-inline">
                                            @Html.AntiForgeryToken()
                                            <button type="submit" class="btn btn-sm btn-primary" title="Move Up">
                                                <i class="bi bi-arrow-up"></i>
                                            </button>
                                        </form>
                                        <form asp-controller="Submodule" asp-action="Delete" asp-route-id="@submodule.SubModuleID" method="post" class="d-inline delete-form ms-1">
                                            @Html.AntiForgeryToken()
                                            <button type="submit" class="btn btn-sm btn-danger" title="Remove">
                                                <i class="bi bi-x"></i>
                                            </button>
                                        </form>

                                        <a href="#" class="btn btn-sm btn-info ms-1" data-bs-toggle="modal" data-bs-target="#submoduleModal-@submodule.SubModuleID" title="View">
                                            <i class="bi bi-eye"></i>
                                        </a>

                                    </div>
                                </li>
                            }
                        </ul>
                    }
                    else
                    {
                        <div class="alert alert-info shadow-sm mt-3">
                            No Submodules created
                        </div>
                    }
                </div>
            </div>
        </div>

        <div class="col-md-4">
            <div class="card shadow-sm">
                <div class="card-body text-center">
					<h5 class="card-title">Tasks</h5> <!-- TASK SECTION -->
                    @if (Model.Tasks.FindAll(t => t.Status == "Not Started").Count != 0)
                    {
                        <ul class="list-group list-group-flush">
                            @foreach (var task in Model.Tasks.FindAll(t => t.Status == "Not Started"))
                            {
                                <li class="list-group-item ItemCard2 ItemCard-NotStarted">

                                    <!-- Modal -->
                                    <div class="modal fade" id="taskModal-@task.TaskID" tabindex="-1" aria-labelledby="taskModalLabel-@task.TaskID" aria-hidden="true">
                                        <div class="modal-dialog modal-dialog-centered">
                                            <div class="modal-content">
                                                <div class="modal-header">
                                                    <h5 class="modal-title" id="taskModalLabel-@task.TaskID">Task Details</h5>
                                                    <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                                                </div>
                                                <div class="modal-body text-start">
                                                    <p><strong>Project ID:</strong> @task.ProjectID</p>
                                                    <p><strong>SubModule ID:</strong> @task.SubModuleID</p>
                                                    <p><strong>Task ID:</strong> @task.TaskID</p>
                                                    <p><strong>Title:</strong> @task.Title</p>
                                                    <p><strong>Description:</strong> @task.Description</p>
                                                    <p><strong>Status:</strong> @task.Status</p>
                                                </div>
                                                <div class="modal-footer">
                                                    <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Close</button>
                                                </div>
                                            </div>
                                        </div>
                                    </div>

                                    <!-- Task card display -->
                                    <p class="mb-1"><strong>Title:</strong> @task.Title</p>
                                    <div class="mt-2">
                                        <form asp-controller="Task" asp-action="MoveStatusUp" asp-route-id="@task.TaskID" method="post" class="d-inline">
                                            @Html.AntiForgeryToken()
                                            <button type="submit" class="btn btn-sm btn-primary" title="Move Up">
                                                <i class="bi bi-arrow-up"></i>
                                            </button>
                                        </form>
                                        <form asp-controller="Task" asp-action="Delete" asp-route-id="@task.TaskID" method="post" class="d-inline delete-form ms-1">
                                            @Html.AntiForgeryToken()
                                            <button type="submit" class="btn btn-sm btn-danger" title="Remove">
                                                <i class="bi bi-x"></i>
                                            </button>
                                        </form>
                                        <a href="#" class="btn btn-sm btn-info ms-1" data-bs-toggle="modal" data-bs-target="#taskModal-@task.TaskID" title="View">
                                            <i class="bi bi-eye"></i>
                                        </a>
                                    </div>
                                </li>
                            }
                        </ul>
                    }
                    else
                    {
                        <div class="alert alert-info shadow-sm mt-3">
                            No Tasks created
                        </div>
                    }

                </div>
            </div>
        </div>
    </div>

	<h4 class="mb-2">In progress</h4> <!-- IN PROGRESS SECTION -->
    <div class="row mb-4">
        <div class="col-md-4">
            <div class="card shadow-sm">
                <div class="card-body text-center">
					<h5 class="card-title">Modules</h5> <!-- MODULES IN PROGRESS SECTION -->
                    @if (Model.Modules.FindAll(m => m.Status == "In Progress").Count != 0)
                    {
                        <ul class="list-group list-group-flush">
                            @foreach (var module in Model.Modules.FindAll(m => m.Status == "In Progress"))
                            {
                                <li class="list-group-item ItemCard2 ItemCard-InProgress">
                                    <!-- Modal -->
                                    <div class="modal fade" id="moduleModal-@module.ModuleID" tabindex="-1" aria-labelledby="moduleModalLabel-@module.ModuleID" aria-hidden="true">
                                        <div class="modal-dialog modal-dialog-centered">
                                            <div class="modal-content">
                                                <div class="modal-header">
                                                    <h5 class="modal-title" id="viewModalLabel-@module.ModuleID">Module Details</h5>
                                                    <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                                                </div>
                                                <div class="modal-body text-start">
                                                    <p><strong>Project ID:</strong> @module.ProjectID</p>
                                                    <p><strong>Module ID:</strong> @module.ModuleID</p>
                                                    <p><strong>Title:</strong> @module.Title</p>
                                                    <p><strong>Description:</strong> @module.Description</p>
                                                    <p><strong>Status:</strong> @module.Status</p>
                                                </div>
                                                <div class="modal-footer">
                                                    <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Close</button>
                                                </div>
                                            </div>
                                        </div>
                                    </div>
                                    <!-- /Modal -->

                                    <p class="mb-1"><strong>Title:</strong> @module.Title</p>
                                    
                                    <div class="mt-2">
                                        <form asp-controller="Module" asp-action="MoveStatusUp" asp-route-id="@module.ModuleID" method="post" class="d-inline">
                                            @Html.AntiForgeryToken()
                                            <button type="submit" class="btn btn-sm btn-primary" title="Move Up">
                                                <i class="bi bi-arrow-up"></i>
                                            </button>
                                        </form>
                                        <form asp-controller="Module" asp-action="MoveStatusDown" asp-route-id="@module.ModuleID" method="post" class="d-inline ms-1">
                                            @Html.AntiForgeryToken()
                                            <button type="submit" class="btn btn-sm btn-light" title="Move down">
                                                <i class="bi bi-arrow-down"></i>
                                            </button>
                                        </form>
                                        <form asp-controller="Module" asp-action="Delete" asp-route-id="@module.ModuleID" method="post" class="d-inline delete-form ms-1">
                                            @Html.AntiForgeryToken()
                                            <button type="submit" class="btn btn-sm btn-danger" title="Remove">
                                                <i class="bi bi-x"></i>
                                            </button>
                                        </form>
                                        <a href="#" class="btn btn-sm btn-info ms-1" data-bs-toggle="modal" data-bs-target="#moduleModal-@module.ModuleID" title="View">
                                            <i class="bi bi-eye"></i>
                                        </a>
                                    </div>
                                </li>
                            }
                        </ul>
                    }
                    else
                    {
                        <div class="alert alert-info shadow-sm mt-3">
                            No Modules created
                        </div>
                    }
                </div>
            </div>
        </div>

        <div class="col-md-4">
            <div class="card shadow-sm">
                <div class="card-body text-center">
					<h5 class="card-title">Submodules</h5> <!-- SUBMODULES IN PROGRESS SECTION -->
                    @if (Model.SubModules.FindAll(m => m.Status == "In Progress").Count != 0)
                    {
                        <ul class="list-group list-group-flush">
                            @foreach (var submodule in Model.SubModules.FindAll(m => m.Status == "In Progress"))
                            {
                                <li class="list-group-item ItemCard2 ItemCard-InProgress">

                                    <!-- Modal start -->
                                    <div class="modal fade" id="submoduleModal-@submodule.SubModuleID" tabindex="-1" aria-labelledby="submoduleModalLabel-@submodule.SubModuleID" aria-hidden="true">
                                        <div class="modal-dialog modal-dialog-centered">
                                            <div class="modal-content">
                                                <div class="modal-header">
                                                    <h5 class="modal-title" id="viewModalLabel-@submodule.SubModuleID">Module Details</h5>
                                                    <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                                                </div>
                                                <div class="modal-body text-start">
                                                    <p><strong>Module ID:</strong> @submodule.ModuleID</p>
                                                    <p><strong>Submodule ID:</strong> @submodule.ProjectID</p>
                                                    <p><strong>Title:</strong> @submodule.Title</p>
                                                    <p><strong>Description:</strong> @submodule.Description</p>
                                                    <p><strong>Status:</strong> @submodule.Status</p>
                                                </div>
                                                <div class="modal-footer">
                                                    <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Close</button>
                                                </div>
                                            </div>
                                        </div>
                                    </div>
                                    <!-- Modal end -->

                                    <p class="mb-1"><strong>Title:</strong> @submodule.Title</p>
                                    <div class="mt-2">
                                        <form asp-controller="Submodule" asp-action="MoveStatusUp" asp-route-id="@submodule.SubModuleID" method="post" class="d-inline">
                                            @Html.AntiForgeryToken()
                                            <button type="submit" class="btn btn-sm btn-primary" title="Move Up">
                                                <i class="bi bi-arrow-up"></i>
                                            </button>
                                        </form>
                                        <form asp-controller="Submodule" asp-action="MoveStatusDown" asp-route-id="@submodule.SubModuleID" method="post" class="d-inline ms-1">
                                            @Html.AntiForgeryToken()
                                            <button type="submit" class="btn btn-sm btn-light" title="Move down">
                                                <i class="bi bi-arrow-down"></i>
                                            </button>
                                        </form>
                                        <form asp-controller="Submodule" asp-action="Delete" asp-route-id="@submodule.SubModuleID" method="post" class="d-inline delete-form ms-1">
                                            @Html.AntiForgeryToken()
                                            <button type="submit" class="btn btn-sm btn-danger" title="Remove">
                                                <i class="bi bi-x"></i>
                                            </button>
                                        </form>

                                        <a href="#" class="btn btn-sm btn-info ms-1" data-bs-toggle="modal" data-bs-target="#submoduleModal-@submodule.SubModuleID" title="View">
                                            <i class="bi bi-eye"></i>
                                        </a>

                                    </div>
                                </li>
                            }
                        </ul>
                    }
                    else
                    {
                        <div class="alert alert-info shadow-sm mt-3">
                            No Submodules created
                        </div>
                    }
                </div>
            </div>
        </div>

        <div class="col-md-4">
            <div class="card shadow-sm">
                <div class="card-body text-center">
                    <h5 class="card-title">Tasks</h5> <!-- TASK IN PROGRESS SECTION -->
                    @if (Model.Tasks.FindAll(t => t.Status == "In Progress").Count != 0)
                    {
                        <ul class="list-group list-group-flush">
                            @foreach (var task in Model.Tasks.FindAll(t => t.Status == "In Progress"))
                            {
                                <li class="list-group-item ItemCard2 ItemCard-InProgress">

                                    <!-- Modal -->
                                    <div class="modal fade" id="taskModal-@task.TaskID" tabindex="-1" aria-labelledby="taskModalLabel-@task.TaskID" aria-hidden="true">
                                        <div class="modal-dialog modal-dialog-centered">
                                            <div class="modal-content">
                                                <div class="modal-header">
                                                    <h5 class="modal-title" id="taskModalLabel-@task.TaskID">Task Details</h5>
                                                    <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                                                </div>
                                                <div class="modal-body text-start">
                                                    <p><strong>Project ID:</strong> @task.ProjectID</p>
                                                    <p><strong>SubModule ID:</strong> @task.SubModuleID</p>
                                                    <p><strong>Task ID:</strong> @task.TaskID</p>
                                                    <p><strong>Title:</strong> @task.Title</p>
                                                    <p><strong>Description:</strong> @task.Description</p>
                                                    <p><strong>Status:</strong> @task.Status</p>
                                                </div>
                                                <div class="modal-footer">
                                                    <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Close</button>
                                                </div>
                                            </div>
                                        </div>
                                    </div>

                                    
                                    <p class="mb-1"><strong>Title:</strong> @task.Title</p>
                                    <div class="mt-2">
                                        <form asp-controller="Task" asp-action="MoveStatusUp" asp-route-id="@task.TaskID" method="post" class="d-inline">
                                            @Html.AntiForgeryToken()
                                            <button type="submit" class="btn btn-sm btn-primary" title="Move Up">
                                                <i class="bi bi-arrow-up"></i>
                                            </button>
                                        </form>
                                        <form asp-controller="Task" asp-action="MoveStatusDown" asp-route-id="@task.TaskID" method="post" class="d-inline">
                                            @Html.AntiForgeryToken()
                                            <button type="submit" class="btn btn-sm btn-light" title="Move Up">
                                                <i class="bi bi-arrow-down"></i>
                                            </button>
                                        </form>
                                        <form asp-controller="Task" asp-action="Delete" asp-route-id="@task.TaskID" method="post" class="d-inline delete-form ms-1">
                                            @Html.AntiForgeryToken()
                                            <button type="submit" class="btn btn-sm btn-danger" title="Remove">
                                                <i class="bi bi-x"></i>
                                            </button>
                                        </form>
                                        <a href="#" class="btn btn-sm btn-info ms-1" data-bs-toggle="modal" data-bs-target="#taskModal-@task.TaskID" title="View">
                                            <i class="bi bi-eye"></i>
                                        </a>
                                    </div>
                                </li>
                            }
                        </ul>
                    }
                    else
                    {
                        <div class="alert alert-info shadow-sm mt-3">
                            No Tasks created
                        </div>
                    }

                </div>
            </div>
        </div>
    </div>

    <h4 class="mb-2">Completed</h4>
    <div class="row mb-4">
        <div class="col-md-4">
            <div class="card shadow-sm">
                <div class="card-body text-center">
					<h5 class="card-title">Modules</h5> <!-- COMPLETED MODULES SECTION -->
                    @if (Model.Modules.FindAll(m => m.Status == "Completed").Count != 0)
                    {
                        <ul class="list-group list-group-flush">
                            @foreach (var module in Model.Modules.FindAll(m => m.Status == "Completed"))
                            {
                                <li class="list-group-item ItemCard2 ItemCard-Completed">
                                    <!-- Modal -->
                                    <div class="modal fade" id="moduleModal-@module.ModuleID" tabindex="-1" aria-labelledby="moduleModalLabel-@module.ModuleID" aria-hidden="true">
                                        <div class="modal-dialog modal-dialog-centered">
                                            <div class="modal-content">
                                                <div class="modal-header">
                                                    <h5 class="modal-title" id="viewModalLabel-@module.ModuleID">Module Details</h5>
                                                    <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                                                </div>
                                                <div class="modal-body text-start">
                                                    <p><strong>Project ID:</strong> @module.ProjectID</p>
                                                    <p><strong>Module ID:</strong> @module.ModuleID</p>
                                                    <p><strong>Title:</strong> @module.Title</p>
                                                    <p><strong>Description:</strong> @module.Description</p>
                                                    <p><strong>Status:</strong> @module.Status</p>
                                                </div>
                                                <div class="modal-footer">
                                                    <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Close</button>
                                                </div>
                                            </div>
                                        </div>
                                    </div>
                                    <!-- /Modal -->

                                    <p class="mb-1"><strong>Title:</strong> @module.Title</p>
                                    <div class="mt-2">

                                        <form asp-controller="Module" asp-action="MoveStatusDown" asp-route-id="@module.ModuleID" method="post" class="d-inline ms-1">
                                            @Html.AntiForgeryToken()
                                            <button type="submit" class="btn btn-sm btn-light" title="Move down">
                                                <i class="bi bi-arrow-down"></i>
                                            </button>
                                        </form>
                                        <form asp-controller="Module" asp-action="Delete" asp-route-id="@module.ModuleID" method="post" class="d-inline delete-form ms-1">
                                            @Html.AntiForgeryToken()
                                            <button type="submit" class="btn btn-sm btn-danger" title="Remove">
                                                <i class="bi bi-x"></i>
                                            </button>
                                        </form>
                                        <a href="#" class="btn btn-sm btn-info ms-1" data-bs-toggle="modal" data-bs-target="#moduleModal-@module.ModuleID" title="View">
                                            <i class="bi bi-eye"></i>
                                        </a>
                                    </div>
                                </li>
                            }
                        </ul>
                    }
                    else
                    {
                        <div class="alert alert-info shadow-sm mt-3">
                            No Modules created
                        </div>
                    }
                </div>
            </div>
        </div>

        <div class="col-md-4">
            <div class="card shadow-sm">
                <div class="card-body text-center">
					<h5 class="card-title">Submodules</h5> <!-- COMPLETED SUBMODULES SECTION -->
                    @if (Model.SubModules.FindAll(m => m.Status == "Completed").Count != 0)
                    {
                        <ul class="list-group list-group-flush">
                            @foreach (var submodule in Model.SubModules.FindAll(m => m.Status == "Completed"))
                            {
                                <li class="list-group-item ItemCard2 ItemCard-Completed">

                                    <!-- Modal start -->
                                    <div class="modal fade" id="submoduleModal-@submodule.SubModuleID" tabindex="-1" aria-labelledby="submoduleModalLabel-@submodule.SubModuleID" aria-hidden="true">
                                        <div class="modal-dialog modal-dialog-centered">
                                            <div class="modal-content">
                                                <div class="modal-header">
                                                    <h5 class="modal-title" id="viewModalLabel-@submodule.SubModuleID">Module Details</h5>
                                                    <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                                                </div>
                                                <div class="modal-body text-start">
                                                    <p><strong>Module ID:</strong> @submodule.ModuleID</p>
                                                    <p><strong>Submodule ID:</strong> @submodule.ProjectID</p>
                                                    <p><strong>Title:</strong> @submodule.Title</p>
                                                    <p><strong>Description:</strong> @submodule.Description</p>
                                                    <p><strong>Status:</strong> @submodule.Status</p>
                                                </div>
                                                <div class="modal-footer">
                                                    <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Close</button>
                                                </div>
                                            </div>
                                        </div>
                                    </div>
                                    <!-- Modal end -->

                                    <p class="mb-1"><strong>Title:</strong> @submodule.Title</p>
                                    <div class="mt-2">
                                        <form asp-controller="Submodule" asp-action="MoveStatusDown" asp-route-id="@submodule.SubModuleID" method="post" class="d-inline ms-1">
                                            @Html.AntiForgeryToken()
                                            <button type="submit" class="btn btn-sm btn-light" title="Move down">
                                                <i class="bi bi-arrow-down"></i>
                                            </button>
                                        </form>
                                        <form asp-controller="Submodule" asp-action="Delete" asp-route-id="@submodule.SubModuleID" method="post" class="d-inline delete-form ms-1">
                                            @Html.AntiForgeryToken()
                                            <button type="submit" class="btn btn-sm btn-danger" title="Remove">
                                                <i class="bi bi-x"></i>
                                            </button>
                                        </form>

                                        <a href="#" class="btn btn-sm btn-info ms-1" data-bs-toggle="modal" data-bs-target="#submoduleModal-@submodule.SubModuleID" title="View">
                                            <i class="bi bi-eye"></i>
                                        </a>

                                    </div>
                                </li>
                            }
                        </ul>
                    }
                    else
                    {
                        <div class="alert alert-info shadow-sm mt-3">
                            No Submodules created
                        </div>
                    }
                </div>
            </div>
        </div>

        <div class="col-md-4">
            <div class="card shadow-sm">
                <div class="card-body text-center">
					<h5 class="card-title">Tasks</h5> <!-- COMPLETED TASKS SECTION -->
                    @if (Model.Tasks.FindAll(t => t.Status == "Completed").Count != 0)
                    {
                        <ul class="list-group list-group-flush">
                            @foreach (var task in Model.Tasks.FindAll(t => t.Status == "Completed"))
                            {
                                <li class="list-group-item ItemCard2 ItemCard-Completed">

                                    <!-- Modal -->
                                    <div class="modal fade" id="taskModal-@task.TaskID" tabindex="-1" aria-labelledby="taskModalLabel-@task.TaskID" aria-hidden="true">
                                        <div class="modal-dialog modal-dialog-centered">
                                            <div class="modal-content">
                                                <div class="modal-header">
                                                    <h5 class="modal-title" id="taskModalLabel-@task.TaskID">Task Details</h5>
                                                    <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                                                </div>
                                                <div class="modal-body text-start">
                                                    <p><strong>Project ID:</strong> @task.ProjectID</p>
                                                    <p><strong>SubModule ID:</strong> @task.SubModuleID</p>
                                                    <p><strong>Task ID:</strong> @task.TaskID</p>
                                                    <p><strong>Title:</strong> @task.Title</p>
                                                    <p><strong>Description:</strong> @task.Description</p>
                                                    <p><strong>Status:</strong> @task.Status</p>
                                                </div>
                                                <div class="modal-footer">
                                                    <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Close</button>
                                                </div>
                                            </div>
                                        </div>
                                    </div>


                                    <p class="mb-1"><strong>Title:</strong> @task.Title</p>
                                    <div class="mt-2">
                                        
                                        <form asp-controller="Task" asp-action="MoveStatusDown" asp-route-id="@task.TaskID" method="post" class="d-inline">
                                            @Html.AntiForgeryToken()
                                            <button type="submit" class="btn btn-sm btn-light" title="Move Up">
                                                <i class="bi bi-arrow-down"></i>
                                            </button>
                                        </form>
                                        <form asp-controller="Task" asp-action="Delete" asp-route-id="@task.TaskID" method="post" class="d-inline delete-form ms-1">
                                            @Html.AntiForgeryToken()
                                            <button type="submit" class="btn btn-sm btn-danger" title="Remove">
                                                <i class="bi bi-x"></i>
                                            </button>
                                        </form>
                                        <a href="#" class="btn btn-sm btn-info ms-1" data-bs-toggle="modal" data-bs-target="#taskModal-@task.TaskID" title="View">
                                            <i class="bi bi-eye"></i>
                                        </a>
                                    </div>
                                </li>
                            }
                        </ul>
                    }
                    else
                    {
                        <div class="alert alert-info shadow-sm mt-3">
                            No Tasks created
                        </div>
                    }

                </div>
            </div>
        </div>
    </div>
</div>